#ifndef OPERASIROBERTS_H
#define OPERASIROBERTS_H
#include <cmath>
#include <vector>

std::vector<std::vector<int>> Roberts(std::vector<std::vector<int>> input, double treshold);
#endif // OPERASIROBERTS_H
